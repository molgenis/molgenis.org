each article, slug in public.develop._data
    if(slug != 'index')
        if(article.subheading)
            h1 #{article.title}
            p #{article.description}:
        else
            li
                p
                    a(href="#{slug}") #{article.title}
                    |  - #{article.description}