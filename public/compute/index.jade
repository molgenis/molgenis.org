h1 Compute

p MOLGENIS compute is a simple framework to create large workflows building on BASH scripts. It has been mostly been used for next-generation sequencing pipelines.

See wiki for background: http://www.molgenis.org/wiki/ComputeStart

each article, slug in public.compute._data
    if(slug != 'index')
        if(article.subheading)
            h1 #{article.title}
            p #{article.description}:
        else
            li
                p
                    a(href="#{slug}") #{article.title}
                    |  - #{article.description}